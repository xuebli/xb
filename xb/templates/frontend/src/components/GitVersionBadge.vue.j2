<script setup>
import { ref, computed } from 'vue'

const version = ref('v' + (__APP_VERSION__ || '0.0.0'))
const commits = ref(__GIT_COMMITS__ || [])
const showModal = ref(false)

const parseCommitMessage = (message) => {
  if (!message) return []
  
  const lines = message.split('\n')
  const parsed = []
  let isFirstLine = true
  
  for (const line of lines) {
    if (!line.trim()) continue
    
    if (isFirstLine) {
      parsed.push({ level: -1, text: line.trim() })
      isFirstLine = false
      continue
    }
    
    const leadingSpaces = line.match(/^( *)/)[0].length
    const level = Math.floor(leadingSpaces / 2)
    const text = line.trim().replace(/^[-•○▪]\s*/, '')
    
    if (text) {
      parsed.push({ level, text })
    }
  }
  
  return parsed
}

const processedCommits = computed(() => {
  return commits.value.map(commit => ({
    ...commit,
    parsedMessage: parseCommitMessage(commit.message)
  }))
})
</script>

<template>
  <div class="git-version-wrapper">
    <span 
      class="git-version-badge" 
      @click="showModal = commits.length > 0 && true"
      :title="commits.length > 0 ? '点击查看提交记录' : '版本号'"
    >
      {{ version }}
    </span>

    <Teleport to="body">
      <div v-if="showModal" class="git-modal" @click="showModal = false">
        <div class="git-modal-content" @click.stop>
          <div class="git-modal-header">
            <h3>Git 提交记录</h3>
            <button @click="showModal = false" class="git-close-btn">✕</button>
          </div>
          
          <div class="git-commits-container">
            <div v-if="commits.length === 0" class="git-empty">
              暂无提交记录
            </div>
            
            <div v-else class="git-commits">
              <div v-for="c in processedCommits" :key="c.hash" class="git-commit">
                <div class="git-commit-header">
                  <code class="git-hash">{{ c.hash }}</code>
                  <span class="git-date">{{ c.date }}</span>
                </div>
                <div class="git-commit-body">
                  <div 
                    v-for="(line, idx) in c.parsedMessage" 
                    :key="idx"
                    :class="['git-msg-line', line.level === -1 ? 'level-title' : `level-${line.level}`]"
                  >
                    <span v-if="line.level === 0" class="git-bullet">•</span>
                    <span v-else-if="line.level === 1" class="git-bullet sub">○</span>
                    <span v-else-if="line.level >= 2" class="git-bullet sub">▪</span>
                    <span class="git-msg-text">{{ line.text }}</span>
                  </div>
                </div>
                <div class="git-author">{{ c.author }}</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </Teleport>
  </div>
</template>

<style scoped>
.git-version-wrapper {
  display: inline-flex;
  align-items: center;
}

.git-version-badge {
  height: 34px;
  display: inline-flex;
  align-items: center;
  padding: 0 13px;
  border: 1px solid rgba(226, 232, 240, 0.9);
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.82);
  color: #64748b;
  font-size: 0.86rem;
  font-weight: 800;
  cursor: pointer;
  user-select: none;
  box-shadow: 0 8px 20px rgba(15, 23, 42, 0.05);
  transition: background 0.18s ease, color 0.18s ease, transform 0.18s ease;
}

.git-version-badge:hover {
  background: #fff;
  color: #334155;
  transform: translateY(-1px);
}

.git-modal {
  position: fixed;
  inset: 0;
  z-index: 20000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24px;
  background: rgba(15, 23, 42, 0.34);
  backdrop-filter: blur(6px);
  isolation: isolate;
}

.git-modal-content {
  width: min(760px, calc(100vw - 48px));
  max-height: min(78vh, 760px);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  border: 1px solid #e5eaf2;
  border-radius: 10px;
  background: #fff;
  color: #111827;
  box-shadow: 0 24px 70px rgba(15, 23, 42, 0.28);
}

.git-modal-header {
  height: 56px;
  padding: 0 18px 0 20px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex: 0 0 auto;
  border-bottom: 1px solid #e5eaf2;
  background: #fff;
}

.git-modal-header h3 {
  margin: 0;
  color: #111827;
  font-size: 1rem;
  font-weight: 800;
  line-height: 1.2;
}

.git-close-btn {
  width: 32px;
  height: 32px;
  display: grid;
  place-items: center;
  padding: 0;
  border: 0;
  border-radius: 7px;
  background: transparent;
  color: #667085;
  font-size: 1rem;
  cursor: pointer;
}

.git-close-btn:hover { background: #f2f4f8; }

.git-commits-container {
  flex: 1 1 auto;
  min-height: 0;
  overflow-y: auto;
  padding: 14px 20px 20px;
  background: #fff;
}

.git-empty {
  padding: 44px 20px;
  color: #94a3b8;
  text-align: center;
}

.git-commit {
  padding: 14px 0;
  border-bottom: 1px solid #eef2f7;
}

.git-commit:last-child { border-bottom: 0; }

.git-commit-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 9px;
}

.git-hash {
  display: inline-flex;
  align-items: center;
  width: fit-content;
  padding: 2px 8px;
  border-radius: 3px;
  background: #fef3c7;
  color: #d97706;
  font-family: 'Monaco', 'Menlo', monospace;
  font-size: 0.75rem;
  line-height: 1.4;
}

.git-date {
  color: #9ca3af;
  font-size: 0.75rem;
  white-space: nowrap;
}

.git-commit-body { margin-bottom: 8px; }

.git-msg-line {
  display: flex;
  align-items: flex-start;
  margin: 3px 0;
  color: #374151;
  font-size: 0.875rem;
  line-height: 1.8;
  text-align: left;
}

.git-msg-line.level-title {
  margin-top: 0;
  margin-bottom: 8px;
  padding-left: 0;
  color: #1f2937;
  font-size: 0.9375rem;
  font-weight: 600;
}

.git-msg-line.level-0 {
  padding-left: 0;
  color: #374151;
}

.git-msg-line.level-1 {
  padding-left: 24px;
  color: #4b5563;
  font-size: 0.8125rem;
}

.git-msg-line.level-2 {
  padding-left: 48px;
  color: #6b7280;
  font-size: 0.8125rem;
}

.git-msg-line.level-3 {
  padding-left: 72px;
  color: #9ca3af;
  font-size: 0.8125rem;
}

.git-bullet {
  display: inline-block;
  margin-right: 8px;
  color: #667eea;
  flex-shrink: 0;
  font-size: 0.875rem;
}

.git-bullet.sub {
  color: #9ca3af;
  font-size: 0.75rem;
}

.git-msg-text {
  flex: 1;
  min-width: 0;
}

.git-author {
  color: #9ca3af;
  font-size: 0.75rem;
  text-align: left;
}

@media (max-width: 640px) {
  .git-modal { padding: 14px; }
  .git-modal-content { width: calc(100vw - 28px); max-height: 86vh; }
  .git-commit-header { align-items: flex-start; flex-direction: column; }
}

/* === 暗色模式 === */
:root[data-theme='dark'] .git-version-badge {
  background: rgba(30, 35, 50, 0.82);
  border-color: rgba(139, 92, 246, 0.24);
  color: #94a3b8;
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
}
:root[data-theme='dark'] .git-version-badge:hover {
  background: rgba(40, 48, 68, 0.9);
  color: #f1f5f9;
}

:root[data-theme='dark'] .git-modal { background: rgba(0, 0, 0, 0.6); }

:root[data-theme='dark'] .git-modal-content {
  background: #1a1f2e;
  border-color: rgba(139, 92, 246, 0.2);
  color: #e2e8f0;
  box-shadow: 0 24px 70px rgba(0, 0, 0, 0.6);
}

:root[data-theme='dark'] .git-modal-header {
  background: #1a1f2e;
  border-bottom-color: rgba(139, 92, 246, 0.16);
}
:root[data-theme='dark'] .git-modal-header h3 { color: #f8fafc; }

:root[data-theme='dark'] .git-close-btn { color: #94a3b8; }
:root[data-theme='dark'] .git-close-btn:hover { background: rgba(51, 65, 85, 0.6); }

:root[data-theme='dark'] .git-commits-container { background: #1a1f2e; }
:root[data-theme='dark'] .git-empty { color: #64748b; }

:root[data-theme='dark'] .git-commit { border-bottom-color: rgba(139, 92, 246, 0.12); }

:root[data-theme='dark'] .git-hash {
  background: rgba(251, 146, 60, 0.15);
  color: #fb923c;
}

:root[data-theme='dark'] .git-date { color: #64748b; }

:root[data-theme='dark'] .git-msg-line { color: #e2e8f0; }
:root[data-theme='dark'] .git-msg-line.level-title { color: #f8fafc; }
:root[data-theme='dark'] .git-msg-line.level-0 { color: #e2e8f0; }
:root[data-theme='dark'] .git-msg-line.level-1 { color: #cbd5e1; }
:root[data-theme='dark'] .git-msg-line.level-2 { color: #94a3b8; }
:root[data-theme='dark'] .git-msg-line.level-3 { color: #64748b; }

:root[data-theme='dark'] .git-bullet { color: #a78bfa; }
:root[data-theme='dark'] .git-bullet.sub { color: #64748b; }
:root[data-theme='dark'] .git-author { color: #64748b; }
</style>
