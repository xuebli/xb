[% if enable_sudo and sudo_password %]sudo_password: "[[ sudo_password]]"
[% else %]sudo_password: "your_password_here"
[% endif %]
# 服务端口（修改后重启 dev.sh / Electron 立即生效）
ports:
  backend: 8000
  frontend: 5173
